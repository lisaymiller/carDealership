package CarDealership;

//think of enum like a drop down list; creating own data type and then restricted list for options for that data type
public enum VehicleClassification {
    // convention to name enum options in all uppercase
    NEW, USED, OFF_LEASE, SHUTTLE, LOANER, PARTS_RUNNER

}